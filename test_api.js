const fs = require('fs');
const fetch = require('node-fetch'); // May need to use dynamic import if it's node-fetch v3, but we can just use native fetch in node 18+

async function run() {
  console.log("Checking logs...");
}
run();
